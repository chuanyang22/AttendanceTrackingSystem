<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Attendance Tracking System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    @{
        var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    }
    <header>
        <nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-4">
            <div class="container">
                <a class="navbar-brand" asp-controller="Student" asp-action="Index">Attendance Tracking System</a>
                <div class="navbar-nav">
                    <a class="nav-link text-light @(currentController == "Student" ? "active fw-bold text-decoration-underline" : "")"
                       asp-controller="Student" asp-action="Index">Students</a>
                    <a class="nav-link text-light @(currentController == "Class" ? "active fw-bold text-decoration-underline" : "")"
                       asp-controller="Class" asp-action="Index">Classes</a>
                    <a class="nav-link text-light @(currentController == "Enrollment" ? "active fw-bold text-decoration-underline" : "")"
                       asp-controller="Enrollment" asp-action="Index">Enrollments</a>
                    <a class="nav-link text-light @(currentController == "Attendance" ? "active fw-bold text-decoration-underline" : "")"
                       asp-controller="Attendance" asp-action="Index">Attendance Sessions</a>
                    <a class="nav-link text-light @(currentController == "AttendanceReport" ? "active fw-bold text-decoration-underline" : "")"
                       asp-controller="AttendanceReport" asp-action="ClassReport">Reports</a>
                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>